# Powerlevel10k is the only prompt theme.
ZSH_THEME=""

# Adjust this path if Powerlevel10k is installed elsewhere.
source "$HOME/powerlevel10k/powerlevel10k.zsh-theme"
[[ ! -f "$HOME/.p10k.zsh" ]] || source "$HOME/.p10k.zsh"
